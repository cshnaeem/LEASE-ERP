
@{
    ViewBag.Title = "Branch MTD Report";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="card">
    <div class="content-header">
        <div class="card-header">
            <h3 class="card-title">Branch MTD</h3>
            <div class="card-tools">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="#">Report</a></li>
                    <li class="breadcrumb-item active">Branch MTD</li>
                </ol>
            </div>
        </div>
    </div>
    <div class="col-lg-12 col-md-12 col-xs-12">
        @using (Html.BeginForm("BranchMTDReport", "Report", FormMethod.Post, new { target = "_blank", id = "frm" }))
        {
            @Html.AntiForgeryToken()
        <div class="row">
            @*<div class="col-3">
                @Html.Label("City")
                @(Html.Kendo()
                    .DropDownList()
                    .Name("CityId")
                    //.SelectedIndex(0)
                    .OptionLabel("Select City")
                    .DataValueField("CityId")
                    .DataTextField("City")
                    .Events(e =>
                    {
                        e.Change("cityChange");
                    })
                    .Filter(FilterType.Contains)
                    .DataSource(ds =>
                    {
                        ds.Read(read => read.Action("CityList", "Setup"));
                    })
                    .HtmlAttributes(new { @style = "width:100%", required="required", validationmessage = "City is Required" })
                    )
                <span class="k-invalid-msg" data-for="CityId"></span>
            </div>*@
            <div class="col-3">
                @Html.Label("Location")
                @(Html.Kendo()
                    .MultiColumnComboBox()
                    .Name("LocId")
                    .Placeholder("All Locations ...")
                    .DataValueField("LocId")
                    .DataTextField("LocName")
                    .Columns(c =>
                    {
                        c.Add().Field("LocCode").Width("100px");
                        c.Add().Field("LocName").Width("200px");
                    })
                    .FilterFields(new string[] { "LocCode", "LocName" })
                    .Filter(FilterType.Contains)
                    //.CascadeFrom("CityId")
                    .DataSource(d =>    
                    {
                        d.Read(r => r.Action("LocationList", "Setup"));
                    })
                    .HtmlAttributes(new { @style = "width:100%" })
                    )
            </div>
            <div class="col-md-3">
                @Html.Label("WorkingDate")
                @Html.Kendo().DatePicker().Name("WorkingDate").Value(DateTime.Now.Date).HtmlAttributes(new { @style = "width:100%" })
            </div>
                <div class="col-md-3">
                    <br />
                    <button class="k-button margin-10" type="submit">
                        <i class="fas fa-print"></i> Show
                    </button>
                </div>
            </div>
        }
    </div>
</div>
<script>
    //function cityChange() {
    //    $("#LocId").data("kendoMultiColumnComboBox").dataSource.read();
    //}
    //function GetLocations() {
    //    return {
    //        CityId: $('#CityId').val() || 0
    //    }
    //}
    //$(function () {
    //    var validator = $("#frm").kendoValidator().data("kendoValidator");
    //    $("form").submit(function (event) {
    //    });
    //});
</script>