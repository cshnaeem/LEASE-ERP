@{
    ViewBag.Title = "Bank Reconciliation";
}
@using (Html.BeginForm("BankReconciliation", "Report", FormMethod.Post, new { target = "_blank", id = "frm" }))
{
    @Html.AntiForgeryToken()
    <div class="card">
        <div class="content-header">
            <div class="card-header">
                <h3 class="card-title">Bank Reconciliation</h3>
                <div class="card-tools">
                    <ol class="breadcrumb float-sm-right">
                        <li class="breadcrumb-item"><a href="#">Report</a></li>
                        <li class="breadcrumb-item active">Bank Reconciliation</li>
                    </ol>
                </div>
            </div>
        </div>
        <div class="col-lg-12 col-md-12 col-xs-12">
            <div class="row">
                <div class="col-md-3">
                    @Html.Label("Doc Date")
                    @Html.Kendo().DatePicker().Name("DocDate").Value(DateTime.Now.Date).HtmlAttributes(new { @style = "width:100%" })
                </div>
                
                <div class="col-md-3 col-sm-3">
                    @Html.Label("Bank Account")
                    @(Html.Kendo()
                .DropDownList()
                .Name("AccId")
                .OptionLabel("NA")
                .SelectedIndex(0)
                .DataValueField("Id")
                .DataTextField("Name")
                //.Events(e => e.Change("GetCheque"))
                //.Suggest(true)
                .Filter(FilterType.Contains)
                .DataSource(ds =>
                {
                    ds.Read(read => read.Action("SubCodeBankList", "Account"));
                })
                .HtmlAttributes(new { @style = "width:100%" })
                )
                </div>
                <div class="col-md-3 col-sm-3">
                    <br />
                    <button class="k-button k-primary" type="submit">
                        <i class="fas fa-print"></i> Show
                    </button>
                </div>
            </div>
        </div>
    </div>
}
