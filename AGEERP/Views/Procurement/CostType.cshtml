@using AGEERP.Models
@{
    ViewBag.Title = "CostType";
}

<div class="card">
    <div class="content-header">

        <div class="card-header">
            <h3 class="card-title">CostType</h3>
            <div class="card-tools">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="/Procurement">Procurement</a></li>
                    <li class="breadcrumb-item active">CostType</li>
                </ol>
            </div>
        </div>

    </div>

    <div class="row">
        <div class="col-lg-12 col-md-12 col-xs-12">
            @(Html.Kendo()
                 .Grid<CostTypeVM>()
                 .Name("gridCostType")
                 .Columns(columns =>
                 {
                     columns.Bound(c => c.CostTypeId).Filterable(false).Hidden();
                     columns.Bound(c => c.CostType).Title("CostType");
                     columns.Bound(c => c.CapitalHOGL);
                     columns.Bound(c => c.CapitalSHGL);
                     columns.Bound(c => c.RevenueHOGL);
                     columns.Bound(c => c.RevenueSHGL);
                     columns.Bound(c => c.IsService);
                     columns.Bound(c => c.DeprRate);
                     columns.Bound(c => c.AccDeprHOGL);
                     columns.Bound(c => c.AccDeprSHGL);
                     columns.Bound(c => c.DeprHOGL);
                     columns.Bound(c => c.DeprSHGL);
                     columns.Command(c => { c.Edit(); c.Destroy(); }).Width(250);
                 })
                 .Pageable()
                 .Sortable()
                 .Scrollable()
                 .ToolBar(t => { t.Create(); t.Search(); })
                 .Search(s => { s.Field(a => a.CostType); })
                 .Navigatable()
                 .Editable(e => e.Mode(GridEditMode.InLine))
                 .HtmlAttributes(new { style = "height:400px;" })
                 .DataSource(dataSource => dataSource
                 .Ajax()
                 .PageSize(20)
                 .Events(e => e.Error("error_handler"))
                 .Model(model =>
                 {
                     model.Id(m => m.CostTypeId);
                 })
                 .Read(read => read.Action("CostType_Read", "Procurement"))
                 .Create(create => create.Action("CostType_Create", "Procurement"))
                 .Update(update => update.Action("CostType_Update", "Procurement"))
                 .Destroy(update => update.Action("CostType_Destroy", "Procurement"))
                 ))
        </div>
    </div>
</div>
@section Scripts{
    <script type="text/javascript">
        var gridElement = $("#gridCostType");
        $(document).ready(function () {
            resizeGrid();
            //$('#m_10000000').addClass('active');
            //$('#d_10000000').css("display", "block");
            //$('#m_10010000').addClass('active');
            //$('#d_10010000').css("display", "block");
            //$('#m_10010100').addClass('active');
            //$('#a_10010100').addClass('active');
        })

        function resizeGrid() {
            var height = $(window).innerHeight() - $('header').innerHeight() - 120;
            if (height < 200) {
                height = 200;
            }
            else if (height > 910) {
                height = 910;
            }
            $("#gridCostType").css("height", height);
            gridElement.data("kendoGrid").resize();
        }

        $(window).resize(function () {
            resizeGrid();
        });
        function error_handler(e) {
            if (e.errors) {
                var message = "Errors:\n";
                $.each(e.errors, function (key, value) {
                    if ('errors' in value) {
                        $.each(value.errors, function () {
                            message += this + "\n";
                        });
                    }
                });
                var dia = $("#msgBox").data("kendoDialog");
                dia.title("Error");
                dia.content(message);
                dia.open();
            }
        }
    </script>
}



