@using AGEERP.Models
@{
    ViewBag.Title = "Nature";
}

<div class="card">
    <div class="content-header">

        <div class="card-header">
            <h3 class="card-title">Nature</h3>
            <div class="card-tools">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="/Home/Index">Home</a></li>
                    <li class="breadcrumb-item active">Nature</li>
                </ol>
            </div>
        </div>

    </div>

    <div class="row">
        <div class="col-lg-12 col-md-12 col-xs-12">
            @(Html.Kendo()
                 .Grid<ItemNatureVM>()
                 .Name("gridNature")
                 .Columns(columns =>
                 {
                     columns.Bound(c => c.ItemNatureId).Filterable(false).Hidden();
                     columns.Bound(c => c.ItemNature).Title("Nature");
                     columns.Command(c => { c.Edit(); c.Destroy(); }).Width(250);
                 })
                 .Pageable()
                 .Sortable()
                 .Scrollable()
                 .ToolBar(t => { t.Create(); t.Search(); })
                 .Search(s => { s.Field(a => a.ItemNature); })
                 .Navigatable()
                 .Editable(e => e.Mode(GridEditMode.InLine))
                 .HtmlAttributes(new { style = "height:400px;" })
                 .DataSource(dataSource => dataSource
                 .Ajax()
                 .PageSize(20)
                 .Events(e => e.Error("error_handler"))
                 .Model(model =>
                 {
                     model.Id(m => m.ItemNatureId);
                 })
                 .Read(read => read.Action("Nature_Read", "Procurement"))
                 .Create(create => create.Action("Nature_Create", "Procurement"))
                 .Update(update => update.Action("Nature_Update", "Procurement"))
                 .Destroy(update => update.Action("Nature_Destroy", "Procurement"))
                 ))
        </div>
    </div>
</div>
@section Scripts{
    <script type="text/javascript">
        var gridElement = $("#gridNature");
        $(document).ready(function () {
            resizeGrid();
            //$('#m_10000000').addClass('active');
            //$('#d_10000000').css("display", "block");
            //$('#m_10010000').addClass('active');
            //$('#d_10010000').css("display", "block");
            //$('#m_10010100').addClass('active');
            //$('#a_10010100').addClass('active');
        })

        function resizeGrid() {
            var height = $(window).innerHeight() - $('header').innerHeight() - 120;
            if (height < 200) {
                height = 200;
            }
            else if (height > 910) {
                height = 910;
            }
            $("#gridNature").css("height", height);
            gridElement.data("kendoGrid").resize();
        }

        $(window).resize(function () {
            resizeGrid();
        });
        function error_handler(e) {
            if (e.errors) {
                var message = "Errors:\n";
                $.each(e.errors, function (key, value) {
                    if ('errors' in value) {
                        $.each(value.errors, function () {
                            message += this + "\n";
                        });
                    }
                });
                var dia = $("#msgBox").data("kendoDialog");
                dia.title("Error");
                dia.content(message);
                dia.open();
            }
        }
    </script>
}



